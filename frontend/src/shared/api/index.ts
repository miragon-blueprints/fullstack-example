// The hand-written mutator (httpClient) is imported by the generated client via its file path; the
// only hand-authored consumer of this segment is the error type, exposed here as the public API.
export { HttpError } from "./http-client";
